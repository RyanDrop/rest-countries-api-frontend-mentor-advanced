import { MapCountryDataPipe } from './map-country-data.pipe';

describe('MapCountryDataPipe', () => {
  it('create an instance', () => {
    const pipe = new MapCountryDataPipe();
    expect(pipe).toBeTruthy();
  });
});
